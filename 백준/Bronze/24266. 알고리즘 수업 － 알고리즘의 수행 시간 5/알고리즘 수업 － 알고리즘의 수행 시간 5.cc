#include <iostream>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	unsigned long long N;
	cin >> N;
	cout << N * N * N << "\n" << 3 << "\n";

	return 0;
}